# listed-company-analysis 依赖
# 核心依赖
requests>=2.25.0          # HTTP请求
pandas>=1.3.0             # 数据处理

# PDF处理（二选一，推荐PyMuPDF）
PyMuPDF>=1.23.0           # PDF全文提取（更快）
pdfplumber>=0.10.0        # PDF表格提取（备选）

# 金融数据
akshare>=1.12.0           # CSRC辅导数据/港股财务等

# 可选依赖
matplotlib>=3.5.0         # 图表（仅结构图使用）
